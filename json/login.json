{
    "LoginController": 
    "<?php require_once('actors-model.php'); session_start(); $username = $_SESSION['username']; $password = $_SESSION['password']; $status = login($username, $password); if($status=true){ if($status['Role'] == \"User\") {header('location: userhomepage.php'); } else{ echo \"Could not sign-in. Invalid login credentials.\"; } ?>"
}